#if($player2)
  #if($player1score > $player2score)
<h2>$user.getName(), You Did It!!</h2>
  #elseif ($player2score > $player1score)
<h2>$player2.getName(), You Did It!!</h2>
  #else
<h2>Everyones a Winner!</h2>
  #end
<p>$user.getName(): $player1score</p>
<p>$player2.getName(): $player2score</p>
<p>$user.getName()'s Record: $user.getMemoryWins() - $user.getMemoryLosses()</p>
<p>$player2.getName()'s Record: $player2.getMemoryWins() - $player2.getMemoryLosses()</p>
<form action="/memory" method="post">
  <button type="submit" class="btn btn-danger">Rematch!!</button>
</form>
<a href="/memory"><button class="btn btn-info">New Game</button></a>
#else
<h2>You Did It!!</h2>
<div class="row">
  <div class="col-md-6">
    <h4>Your Final Score:</h4>
    <p>$score</p>
    #if ($congrats)
    $congrats
    #end
    <h4>High Score:</h4>
    <p>$highScore</p>
    <a href="/memory"><button class="btn btn-danger">Play Again</button></a>
  </div>
  <div class="col-md-6">
    <h3 class="gameHeaderText">Current High Scores:</h3>
    <ul>
    #foreach($useer in $users)
      <li>$useer.getName(): $useer.getMemoryHighScore()</li>
    #end
    </ul>
  </div>
</div>
#end
